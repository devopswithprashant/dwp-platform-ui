apiVersion: v1
kind: Secret
metadata:
  name: grafana-admin
  namespace: monitoring
  labels:
    app: grafana
type: Opaque
stringData:
  # Change before production. Grafana reads GF_SECURITY_ADMIN_PASSWORD from env.
  admin-user: admin
  admin-password: changeme
