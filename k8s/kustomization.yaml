apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespaces.yaml
  - ui/deployment.yaml
  - ui/service.yaml
  # Uncomment if Prometheus Operator is installed:
  # - ui/servicemonitor.yaml

# Central Prometheus + Grafana + Blackbox: kubectl apply -k monitoring/
